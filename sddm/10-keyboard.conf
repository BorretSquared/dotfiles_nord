# Make APT the first layout in the SDDM greeter.
# Install: sudo cp sddm/10-keyboard.conf /etc/sddm.conf.d/10-keyboard.conf
# Requires xkb_symbols/apt (and optionally apt_de) installed under /usr/share/X11/xkb/symbols/
[General]
GreeterEnvironment=XKB_DEFAULT_LAYOUT=apt,apt_de,us
