# Theme toggle assets

Used by `~/.config/hypr/scripts/theme_toggle.sh` (Super+Z).

| File | Role |
|------|------|
| `mako-light` / `mako-dark` | Notification daemon colors |
| `quickcss-light.css` / `quickcss-dark.css` | Vesktop / Equibop Nordic QuickCSS |

Wallpapers (paths in `light_wallpaper.txt` / `dark_wallpaper.txt`):

- Light: `~/Documents/backgrounds/GreatWallStairs.jpg`
- Dark: `~/Documents/backgrounds/misty_mountains.jpg`

Default state: **light** (`theme_state`).
